{
    "builds":[
        {
            "src":"snowball/app.py",
            "use":"@vercel/python"
        }

    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"snowball/app.py"
        }
    ]
}